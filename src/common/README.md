# common

`common` 用于放置跨业务模块复用的“纯通用层”能力。

## 职责

- 提供不依赖具体业务领域的公共代码。
- 统一后端项目的异常处理、请求处理和类型复用方式。
- 降低模块间重复实现的概率。

## 适合放什么

- 自定义装饰器、守卫、拦截器、管道、过滤器。
- 通用 DTO、常量、类型定义、工具函数。
- 统一异常基类或错误码约定。

## 边界

- 如果代码与某个具体业务强绑定，优先放到对应 `modules` 内。
- 如果代码是多个业务共享的外部客户端或共享服务，优先考虑放到 `shared`。
